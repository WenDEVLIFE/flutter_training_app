import 'package:flutter/cupertino.dart';

class NoliView extends StatelessWidget {

  @override
  Widget build(BuildContext context) {

    return Container(
      child: Text('Noli View'),
    );
  }
}